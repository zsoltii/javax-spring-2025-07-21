hazelcast:
  instance-name: employees
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: false
  map:
    default:
      time-to-live-seconds: 3600
      max-idle-seconds: 1800
